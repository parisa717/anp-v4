query GetArea($id: UUID!) {
  getArea(id: $id) {
    id
    code
    crm
    dms
    name
    isActive
    address {
      country {
        id
        name
      }
      postCode
      city
      address
    }
  }
}
